<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css?family=Odibee Sans|Noto Sans Korean"
/>
<link rel="stylesheet" href="/app.css" />

<div class="flex-container">
  <div class="container">
    <h1>THIS IS HTML</h1>
    <body>
      <div>
        <p>Enter your email to join <b> <%=count %> </b> Others.</p>
      </div>
      <div>
        <form action="/register" method="post">
          <input
            type="text"
            name="email"
            class="form"
            placeholder="Enter your email"
          />
          <button type="submit">Join Now</button>
        </form>
      </div>
    </body>
  </div>
</div>

  <div class="flex-container">
    <div class="container">
      <h1>JOIN US</h1>
      <p class="lead">
        Enter your email to join <strong><%= count %></strong> others on our
        waitlist. We are 100% not a cult.
      </p>
      <form method="POST" action="/register">
        <input
          type="text"
          name="email"
          class="form"
          placeholder="Enter Your Email"
        />
        <button>Join Now</button>
      </form>
    </div>
  </div>
</div>
